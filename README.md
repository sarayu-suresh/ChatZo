# ChatZo

## How it Works ?
1. Step 1 - Create a user account and login 
2. Step 2 - Search for user using their unique number
3. Step 3 - Send messages to that user

## Dependencies
        "firebase": "^9.13.0",
        "react": "^18.0.0",
        "react-dom": "^18.0.0",
        "react-router-dom": "^6.4.3",
        "react-scripts": "5.0.1",
        "sass": "^1.55.0",
        "uuid": "^9.0.0",
        "web-vitals": "^2.1.4"

## How to Run
- clone the repository
- Terminal command
  ```
  `npm start`
  ```
  Runs the app in the development mode.\
  Open [http://localhost:3000](http://localhost:3000) to view it in your browser.